import { MyChangeDirective } from './my-change.directive';

describe('MyChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new MyChangeDirective();
    expect(directive).toBeTruthy();
  });
});
